#!/usr/bin/env bash
set -euo pipefail

GARDEN="${SECONDBRAIN:-$HOME/repositories/garden}"

if [[ ! -d "$GARDEN" ]]; then
  echo "error: garden not found at $GARDEN" >&2
  exit 1
fi

cd "$GARDEN"
mise run sync
